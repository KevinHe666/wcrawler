<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wuyi.wcrawler.mapper.CrawlerUrlMapper">
    <resultMap id="BaseResultMap" type="com.wuyi.wcrawler.entity.CrawlerUrl">
        <!--注意这里的property对应了bean字段名称-->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="url" property="url" jdbcType="VARCHAR"/>
        <result column="parent" property="parent" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
    </resultMap>

    <select id="selectBySharding" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT * FROM crawler_url WHERE status = 0 AND mod(id, #{shardingTotalCount}) = #{shardingItem} LIMIT 3
    </select>

    <update id="updateUrlStatus">
        UPDATE crawler_url SET status = 1 WHERE id IN
        <foreach collection="list"  index="index"  item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
    </update>
</mapper>